<Page
    x:Class="QMSFlowDoc.Client.Views.EQAConfigurationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:dto="using:QMSFlowDoc.Shared.DTOs"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Configuración de Control Externo" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="Gestiona proveedores y esquemas de evaluación externa de calidad." Style="{StaticResource BodyTextBlockStyle}" Foreground="Gray" />
        </StackPanel>

        <Pivot Grid.Row="1">
            <!-- Providers Tab -->
            <PivotItem Header="Proveedores">
                <Grid Padding="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <CommandBar Grid.Row="0" DefaultLabelPosition="Right" Background="Transparent" HorizontalAlignment="Left" OverflowButtonVisibility="Collapsed">
                        <AppBarButton Icon="Add" Label="Nuevo Proveedor" Click="AddProvider_Click" />
                        <AppBarButton Icon="Refresh" Label="Actualizar" Click="RefreshProviders_Click" />
                    </CommandBar>

                    <ListView x:Name="ProvidersList" Grid.Row="1" Padding="0,12" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="dto:EQAProviderDto">
                                <Grid Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind Code}" FontSize="12" Foreground="Gray" Visibility="{x:Bind Code, Converter={StaticResource StringVisibilityConverter}}" />
                                        <TextBlock Text="{x:Bind ContactInfo}" FontSize="12" Foreground="Gray" TextTrimming="CharacterEllipsis" Visibility="{x:Bind ContactInfo, Converter={StaticResource StringVisibilityConverter}}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                        <Button Content="Editar" Click="EditProvider_Click" Tag="{x:Bind}" />
                                        <ToggleSwitch IsOn="{x:Bind IsActive, Mode=OneWay}" OffContent="Inactivo" OnContent="Activo" IsEnabled="False" MinWidth="0" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>

            <!-- Schemes Tab -->
            <PivotItem Header="Esquemas">
                <Grid Padding="0,12,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="16" Margin="0,0,0,12">
                         <ComboBox x:Name="ProviderFilterCombo" Header="Filtrar por Proveedor" Width="250" 
                                   SelectionChanged="ProviderFilter_SelectionChanged" DisplayMemberPath="Name" />
                         <CommandBar DefaultLabelPosition="Right" Background="Transparent" VerticalAlignment="Bottom">
                            <AppBarButton Icon="Add" Label="Nuevo Esquema" Click="AddScheme_Click" />
                            <AppBarButton Icon="Refresh" Label="Actualizar" Click="RefreshSchemes_Click" />
                        </CommandBar>
                    </StackPanel>

                    <ListView x:Name="SchemesList" Grid.Row="1" Padding="0,12" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="dto:EQASchemeDto">
                                <Grid Padding="12" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Spacing="4">
                                        <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                        <TextBlock Text="{x:Bind ProviderName}" FontSize="12" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" />
                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                            <TextBlock Text="{x:Bind Matrix}"  FontSize="12" Foreground="Gray" />
                                            <TextBlock Text="{x:Bind Periodicity}" FontSize="12" Foreground="Gray" />
                                        </StackPanel>
                                    </StackPanel>
                                    <Button Grid.Column="1" Content="Editar" Click="EditScheme_Click" Tag="{x:Bind}" VerticalAlignment="Center" />
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
